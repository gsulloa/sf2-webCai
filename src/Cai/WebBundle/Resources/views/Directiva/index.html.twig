{% extends 'CaiWebBundle::base.html.twig' %}

{% block body -%}
    <h1>Directiva list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Anno</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('directiva_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.anno }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('directiva_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('directiva_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('directiva_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
