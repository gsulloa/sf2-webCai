{% extends 'CaiWebBundle::base.html.twig' %}

{% block body -%}
    <h1>Imagen</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Filename</th>
                <td>{{ entity.filename }}</td>
            </tr>
            <tr>
                <th>Small</th>
                <td>
                    <a target="_blank" href=" {{ asset('uploads/biblioteca/imagenes/') }}{{ entity.filenamebinary }}/small-{{ entity.filename }}"><img src="{{ asset('uploads/biblioteca/imagenes/') }}{{ entity.filenamebinary }}/small-{{ entity.filename }}"></a>
                </td>
            </tr>
            <tr>
                <th>Medium</th>
                <td>
                    <a target="_blank" href="{{ asset('uploads/biblioteca/imagenes/') }}{{ entity.filenamebinary }}/medium-{{ entity.filename }}" ><img src="{{ asset('uploads/biblioteca/imagenes/') }}{{ entity.filenamebinary }}/medium-{{ entity.filename }}"></a>
                </td>
            </tr>
            <tr>
                <th>Original</th>
                <td>
                    <a target="_blank" href="{{ asset('uploads/biblioteca/imagenes/') }}{{ entity.filenamebinary }}/{{ entity.filename }}"> <img src="{{ asset('uploads/biblioteca/imagenes/') }}{{ entity.filenamebinary }}/{{ entity.filename }}"></a>
                </td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('imagen') }}">
            Back to the list
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
