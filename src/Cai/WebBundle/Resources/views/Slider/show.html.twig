{% extends 'CaiWebBundle::base.html.twig' %}

{% block body -%}
    <div class="row">
        <h1>Slider</h1>

        <table class="table table-bordered">
            <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ entity.id }}</td>
                </tr>
                <tr>
                    <th>Titulo</th>
                    <td>{{ entity.titulo }}</td>
                </tr>
            </tbody>
        </table>
    </div>

    <div class="row">
        <hr>
        <h2>Slides</h2>
        <button class="btn btn-default text-right">Agregar Slide</button>
        <form action="#" method="post" id="slides-form-generator">
            {% if previous_slides %}
            {% else %}
            {% endif %}
        </form>
    </div>

        <ul class="record_actions">
    <li>
        <a href="{{ path('slider') }}">
            Volver
        </a>
    </li>
    <li>
        <a href="{{ path('slider_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
{% block javascripts %}
{% endblock %}